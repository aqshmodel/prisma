
import type { OSContent } from './os';
import { OS_CONTENT_EXPANDED } from './os';

// Re-export types and data for compatibility
export type { OSContent };
export const OS_CONTENT = OS_CONTENT_EXPANDED;
